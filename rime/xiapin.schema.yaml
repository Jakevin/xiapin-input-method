# Rime schema
# encoding: utf-8

schema:
  schema_id: xiapin
  name: "蝦拼"
  version: "0.1.18"
  author:
    - "蝦拼輸入法 project"
  description: |
    Experimental Pinyin plus openxiami root schema.
    v0.1.18: performance-oriented defaults for macOS Squirrel.

switches:
  - name: ascii_mode
    reset: 0
    states: ["中文", "西文"]
  - name: full_shape
    states: ["半形", "全形"]

engine:
  processors:
    - ascii_composer
    - recognizer
    - key_binder
    - speller
    - punctuator
    - selector
    - navigator
    - express_editor
  segmentors:
    - ascii_segmentor
    - matcher
    - abc_segmentor
    - punct_segmentor
    - fallback_segmentor
  translators:
    - punct_translator
    - table_translator
    - script_translator@pinyin
  filters:
    - uniquifier
    - lua_filter@*boshiamy_comment

speller:
  alphabet: "abcdefghijklmnopqrstuvwxyz[];'"
  delimiter: " '"

translator:
  dictionary: xiapin.extended
  enable_sentence: false
  enable_user_dict: false
  # 字根補全保留（嘸蝦米需要），但關閉過重的 sentence
  enable_completion: true
  initial_quality: 1000
  # 限制編碼提示長度，減少 menu 負擔
  spelling_hints: 4

pinyin:
  dictionary: luna_pinyin
  # 組詞保留；關閉 user_dict 減少磁碟 I/O
  enable_sentence: true
  enable_user_dict: false
  initial_quality: 100
  # 句段過長時較慢：限制最大音節（Rime 支援）
  max_homophones: 50
  preedit_format:
    - xform/([nl])v/$1ü/
    - xform/([nl])ue/$1üe/
    - xform/([jqxy])v/$1u/

menu:
  page_size: 7

punctuator:
  import_preset: default

key_binder:
  bindings:
    - { when: composing, accept: Control+p, send: Up }
    - { when: composing, accept: Control+n, send: Down }
    - { when: composing, accept: Control+b, send: Left }
    - { when: composing, accept: Control+f, send: Right }
    - { when: composing, accept: Control+a, send: Home }
    - { when: composing, accept: Control+e, send: End }
    - { when: composing, accept: Control+d, send: Delete }
    - { when: composing, accept: Control+h, send: BackSpace }
    - { when: composing, accept: Control+g, send: Escape }
    - { when: composing, accept: Control+bracketleft, send: Escape }
    - { when: composing, accept: Control+v, send: Page_Down }
    - { when: composing, accept: Alt+v, send: Page_Up }
    - { when: has_menu, accept: space, send: space }
    - { when: paging, accept: comma, send: Page_Up }
    - { when: has_menu, accept: period, send: Page_Down }
    - { when: always, accept: Shift+space, select: xiapin_english }
    - { when: always, accept: Control+Shift+grave, select: .next }
    - { when: always, accept: Control+grave, select: .next }

recognizer:
  import_preset: default

ascii_composer:
  good_old_caps_lock: true
  switch_key:
    Shift_L: commit_code
    Shift_R: commit_code
    Control_L: noop
    Control_R: noop
    Caps_Lock: clear
    Eisu_toggle: clear
