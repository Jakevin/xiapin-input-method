# 蝦注拼 v0.2.16

## 中 / 英 / 符 三態輪流

底列改為**單一模式鍵**，點一下輪流：

```
中 → 英 → 符 → 中 → …
```

- 鍵帽顯示目前模式：`中` / `英` / `符`
- 拿掉獨立「符」鍵 → 底列更鬆、空格更寬
- 符號層左下也是同一顆模式鍵（顯示 `符`，再點回 `中`）

https://github.com/Jakevin/xiapin-input-method/releases/tag/v0.2.16
